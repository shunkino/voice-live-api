# ── Runtime ────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Foundry hosted-agent SDK — required for InvocationAgentServerHost.
azure-ai-agentserver-invocations

# Optional: Azure AI projects + identity for Foundry deployment.
# azure-ai-projects>=2.0.0b3
# azure-identity

# Optional: Voice Live integration patterns.
# azure-ai-voicelive>=1.2.0b4

# ── Test ───────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
websockets>=12.0
